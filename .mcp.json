{
  "mcpServers": {
    "agentic-deep-research": {
      "command": "python3",
      "args": [
        "tools/mcp_server.py"
      ],
      "env": {
        "TAVILY_API_KEY": "${TAVILY_API_KEY}",
        "BRAVE_API_KEY": "${BRAVE_API_KEY}"
      }
    }
  }
}
